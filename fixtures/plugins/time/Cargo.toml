[package]
name = "time"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib", "dylib"]

[package.metadata.cordis]
plugin_path = "time"
abi_kind = "rust"
declared_nodes = ["time_now"]
children = []

[package.metadata.cordis.abi_fingerprint]
crate_hash = "crate_time_v1"
api_hash = "api_v2"

[dependencies]
cordis-plugin-sdk = { path = "../../../crates/cordis-plugin-sdk" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
