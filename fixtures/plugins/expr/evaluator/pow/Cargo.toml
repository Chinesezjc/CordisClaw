[package]
name = "expr_evaluator_pow"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib", "dylib"]

[package.metadata.cordis]
plugin_path = "expr/evaluator/pow"
abi_kind = "rust"
declared_nodes = ["expr_pow"]
children = []

[package.metadata.cordis.abi_fingerprint]
crate_hash = "crate_expr_pow_v1"
api_hash = "api_v2"

[dependencies]
cordis-plugin-sdk = { path = "../../../../../crates/cordis-plugin-sdk" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[workspace]
