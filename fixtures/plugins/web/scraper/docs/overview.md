# web/scraper

Web scraper child plugin — search the web and optionally fetch page content.

## Nodes

- `web_scraper` — search using Brave or Bing, then optionally fetch each result's page text.

## Requirements

- `BRAVE_API_KEY` or `BING_API_KEY` environment variable must be set for search functionality.
