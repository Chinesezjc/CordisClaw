{
  "plugin_id": "web",
  "plugin_path": "web",
  "plugin_version": "0.1.0",
  "abi_version": 2,
  "command_name": null,
  "nodes": [
    {
      "id": "web_search",
      "summary": "Search the web using DeepSeek Anthropic-compatible API. Returns structured results (title + URL per result) plus an AI summary. Use web_fetch to verify specific pages.",
      "input_schema": {
        "type": "object",
        "required": [
          "node_id",
          "query"
        ],
        "properties": {
          "node_id": {
            "type": "string",
            "const": "web_search"
          },
          "query": {
            "type": "string",
            "description": "Search query"
          }
        }
      },
      "output_schema": {
        "type": "object",
        "properties": {
          "ok": {
            "type": "boolean"
          },
          "text": {
            "type": [
              "string",
              "null"
            ],
            "description": "Structured search results (numbered list with titles + URLs) + AI summary"
          },
          "error": {
            "type": [
              "string",
              "null"
            ]
          }
        }
      },
      "side_effects": [
        "makes HTTP request to DeepSeek Anthropic-compatible endpoint with native web_search tool"
      ],
      "failure_modes": [
        "api key not configured",
        "network unavailable",
        "rate limited"
      ],
      "node_type": "router",
      "agent_accessible": true
    },
    {
      "id": "web_fetch",
      "summary": "Fetch a web page and return plain-text content (HTML tags stripped). Max 8000 chars. Only http/https allowed.",
      "input_schema": {
        "type": "object",
        "required": [
          "node_id",
          "url"
        ],
        "properties": {
          "node_id": {
            "type": "string",
            "const": "web_fetch"
          },
          "url": {
            "type": "string",
            "description": "URL to fetch"
          }
        }
      },
      "output_schema": {
        "type": "object",
        "properties": {
          "ok": {
            "type": "boolean"
          },
          "text": {
            "type": [
              "string",
              "null"
            ]
          },
          "truncated": {
            "type": "boolean"
          },
          "error": {
            "type": [
              "string",
              "null"
            ]
          }
        }
      },
      "side_effects": [
        "makes HTTP GET request to the target URL"
      ],
      "failure_modes": [
        "invalid URL",
        "network timeout",
        "localhost/private IP blocked"
      ],
      "node_type": "router",
      "agent_accessible": true
    }
  ],
  "system_hint": null
}