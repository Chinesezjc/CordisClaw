# shell

External CordisClaw terminal plugin exported as a Rust dylib.
It preserves the current interactive REPL and one-shot command semantics while allowing runtime to invoke it through the generic plugin path.
